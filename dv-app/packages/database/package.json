{
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "seed:articles": "ts-node --project tsconfig.json seeds/articles.seed.ts",
    "db:migrate": "ts-node --project tsconfig.json scripts/migrationScript.ts",
    "neondb:migrate": "psql $NEON_DB_URL -f migrations/001_create_articles.sql"
  }
}
